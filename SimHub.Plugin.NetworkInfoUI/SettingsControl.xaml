<UserControl x:Class="SimHub.Plugin.NetworkInfo.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:ui="clr-namespace:SimHub.Plugins.UI;assembly=SimHub.Plugins"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header: title + Save settings row, then full-width info text -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Network Info Plugin"
                           FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <styles:SHButtonPrimary x:Name="SaveButton" Height="28" VerticalAlignment="Center"
                                             Click="SaveButton_Click">Save settings</styles:SHButtonPrimary>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap"
                           Margin="0,8,0,0" Opacity="0.8">
                    Shows the current local network status and lets you set how often it refreshes.
                    Nothing here calls out to the internet.
                </TextBlock>
            </Grid>

            <!-- Bordered, content-sized section: current live status -->
            <Border Grid.Row="1" BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="1"
                    CornerRadius="4" Margin="0,0,0,12">
                <styles:SHSection Title="Current network status" ShowSeparator="True">
                    <DataGrid x:Name="StatusGrid" AutoGenerateColumns="False" HeadersVisibility="Column"
                              CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="True"
                              CanUserSortColumns="False" IsReadOnly="True" GridLinesVisibility="None"
                              HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Property" Binding="{Binding Property}" Width="220"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </styles:SHSection>
            </Border>

            <!-- Bordered, content-sized section: refresh interval setting -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="1"
                    CornerRadius="4" Margin="0,0,0,12">
                <styles:SHSection Title="Settings" ShowSeparator="True">
                    <StackPanel>
                        <ui:TitledSlider x:Name="RefreshIntervalSlider" Title="Refresh interval (seconds)"
                                         Minimum="1" Maximum="30" Value="5"
                                         Width="320" HorizontalAlignment="Left"/>
                        <TextBlock TextWrapping="Wrap" Margin="0,8,0,0" Opacity="0.8">
                            How often the local network info (and the status table above) is re-read.
                            Click "Save settings" above to apply and persist a new value.
                        </TextBlock>
                    </StackPanel>
                </styles:SHSection>
            </Border>

            <TextBlock Grid.Row="3" x:Name="StatusText" TextWrapping="Wrap" Opacity="0.8"/>
        </Grid>
    </ScrollViewer>
</UserControl>
